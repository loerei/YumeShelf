# 02.4.2.2.1.1 — Library Grid Folder Accordion Rendering & Multi-Grid Binding ($B$)

## Epic
PRD: Sort By Folder (Variant C) & Game Card Drag-Drop Migration to SortableJS

## What to build
Implement Variant C folder view accordion rendering, collapsed state tracking, grid visibility toggles, and multi-grid SortableJS binding in `src/renderer/library-grid.ts`:
1. Parameter Seams on `createLibraryGridController`:
   - Accept injected `electronAPI: any`. Prohibit controllers from accessing ambient global `(window as any).electronAPI`.
   - Accept injected `refs`: `refs.gameGridWrapper` alongside existing grid elements.
   - Accept injected seams: `getLibraryConfig?: () => any` and `bindGrids?: (containers: HTMLElement[]) => void`.
   - Preserve `createLibraryItem: (item, options) => libraryRuntime.createLibraryItem(item, options)` for card and section rendering.
2. State Management & Grid Lifecycle:
   - Maintain in-memory `collapsedSections = new Set<string>()` state tracking collapsed folder paths across render passes.
   - Maintain headless runtime safety (relying on `typeof localStorage !== 'undefined'` and `requestAnimationFrame` fallback to `queueMicrotask` established in Ticket `01.3.2`) ensuring Vitest test suites execute deterministically in Node.js.
    - Manage grid and separator visibility: set `refs.unfavGrid.style.display = 'none'` in folder mode, restore `''` in flat sort modes. In folder sort mode, set `refs.favGrid.style.display = (activeTabId === 'all' || activeTabId === 'fav') ? '' : 'none'` (restoring `''` in flat sort modes); on individual folder tabs (`activeTabId !== 'all' && activeTabId !== 'fav'`), `refs.favGrid` must be hidden (`'none'`) and excluded from `options.bindGrids` to eliminate phantom spacing and ghost drop targets above single folder views. Symmetrically apply tab-scoped section visibility culling to all `.library-section` containers: on tab `'fav'`, hide all folder sections (`sectionEl.style.display = 'none'`); on an individual folder tab (`activeTabId === '<folder-id>'`), set the matching folder section to `style.display = ''` and hide all other sibling folder sections (`style.display = 'none'`); on tab `'all'`, restore all canonical root sections to `style.display = ''`. On tab `'all'` when `favoritesCount === 0`, `refs.favGrid` must retain an accessible drop target container (maintaining `.drop-zone` with `min-height: 40px`) so SortableJS can calculate pointer bounding box intersections for cross-zone card drops from folder sections into favorites. Set `refs.separator.style.display` to `'flex'` ONLY on tab `'all'` when `favoritesSection.games.length > 0` AND `folderSections.length > 0`; set to `'none'` otherwise (on tab `'fav'`, individual folder tabs, or when favorites count is 0). In flat sort modes, preserve existing `(favorites.length > 0 && nonFavorites.length > 0) ? 'flex' : 'none'`. When in "By Folder" sort mode, dynamically assign `role="tabpanel"` and `aria-labelledby` referencing the active shelf tab element ID to `refs.gameGridWrapper`; in flat sort modes, remove `role="tabpanel"` and `aria-labelledby`.
   - Update `queryRenderedCards()` in `src/renderer/library-grid.ts` to query all rendered cards within `refs.gameGridWrapper.querySelectorAll('.game-card[data-game-key]')` (or dynamically aggregate from `refs.favGrid` and all rendered `.library-section .game-grid` containers), rather than querying only `refs.favGrid` and `refs.unfavGrid`, ensuring `captureCardRects()` and `animateReorderedCards()` track card coordinates and execute FLIP transitions correctly across both flat and folder sort modes. In `captureCardRects()`, filter out non-visible cards (skipping cards where `card.offsetParent === null`, cards inside collapsed `.library-section.is-collapsed` sections, or cards inside `inert` containers) prior to querying `card.getBoundingClientRect()`, capturing coordinates strictly for visible cards participating in active layout transitions. Enforce container-scoped composite keying in card rect capture and reorder animation routines, combining the container zone identity (distinguishing the pinned favorites container from folder section containers, e.g. composite key combining enclosing grid container ID or folder path with `gameKey`) with the game key. Dual-placed cards sharing the same game key across `refs.favGrid` and dynamic folder section grids must record distinct spatial coordinates, preventing coordinate overwrite collisions and eliminating erroneous cross-screen jump animations during library re-sort passes. In `animateReorderedCards`, enforce a strict two-phase batched layout execution pattern: (1) Batch Read Phase filters visible cards only (skipping cards where `card.offsetParent === null`, cards inside collapsed `.is-collapsed` accordion sections, or cards whose `previousRect.width === 0 && previousRect.height === 0`), queries `card.getBoundingClientRect()`, computes displacement vectors (`deltaX`, `deltaY`), and stages items with `Math.abs(deltaX) >= 1 || Math.abs(deltaY) >= 1`; (2) Batch Play Phase iterates through the staged items and invokes `card.animate(...)` exclusively in the second pass, strictly prohibiting interleaving layout geometry reads (`getBoundingClientRect()`) with Web Animation API writes (`card.animate(...)`).
     - Section DOM Teardown & In-Place Reconciliation Lifecycle: Unconditional clearance of `.library-section` DOM containers from `refs.gameGridWrapper` (`refs.gameGridWrapper.querySelectorAll('.library-section').forEach((el) => el.remove())`) is strictly restricted to: (1) Sort mode transitions leaving `'folder'` mode (switching to flat sort modes `'date'`, `'played'`, `'az'`, or `'custom'`), and (2) Controller teardown / destruction (`destroy()`). When `currentSortMode === 'folder'`, `renderLibraryGrid` performs in-place section reconciliation keyed by `dataset.folderPath` (matching `canonicalRoots`): existing section containers, headers, badges, and child grids are preserved and updated in-place; section DOM nodes are removed only if their corresponding root path is deleted from `canonicalRoots`; new section DOM nodes are appended only when new roots are added; and child game cards within each section are reconciled in-place. During in-place section reconciliation, the controller MUST iterate through `canonicalRoots` in sequential order, matching existing `.library-section` DOM containers by `dataset.folderPath` and re-appending them into `refs.gameGridWrapper` (strictly after `.folder-accordion-toolbar`) via `refs.gameGridWrapper.appendChild(sectionEl)` to ensure physical DOM element order strictly mirrors the canonical order of `canonicalRoots`. Symmetrically, child game cards within each section (and within `refs.favGrid`) must be reconciled in-place: to prevent $O(N^2)$ quadratic DOM traversals, the controller MUST pre-index existing card DOM elements into an $O(1)$ lookup collection (`const existingCardMap = new Map<string, HTMLElement>()` populated via a single `container.querySelectorAll('.game-card[data-game-key]')` sweep) prior to iterating over `section.games` or `favoritesSection.games`. Matching existing DOM elements MUST perform an $O(1)$ map lookup (`existingCardMap.get(gameKey)`), re-inserting them into their respective `.game-grid` container to guarantee physical DOM child order strictly matches `section.games` (or `favoritesSection.games`) without repeated inner `container.querySelector(...)` sweeps; if a model game entry in `section.games` or `favoritesSection.games` does not have a matching existing DOM card in the target container (such as newly favorited games requiring dual placement, games added via `onGameAdded`, or initial folder section renders), the controller MUST instantiate a new card DOM element via `renderItem(game)` (calling `createLibraryItem(game, itemOptions)`) and insert it at the corresponding index; and explicitly track matched card DOM elements against the model, removing unreferenced, surplus, or duplicate card DOM elements (`cardEl.remove()`) to purge phantom nodes from cross-zone drag operations. Because native `appendChild` on an existing DOM node moves it without unmounting or re-creating it, focused elements, active inputs, and event listeners remain undisturbed while guaranteeing that physical DOM order synchronizes immediately with `oldOrder` upon reorder failure rollback (`onReorderFailed`) and external configuration updates (`options.onConfigUpdated`). Pre-existing `.folder-accordion-toolbar` containers are unmounted only when transitioning out of tab `'all'` or during `destroy()`, preventing toolbar duplication and eliminating destructive layout thrashing.
    - Focus Continuity Across Render Passes: Reordering and section render passes preserve existing DOM elements keyed by container-scoped composite keys (combining container zone identity with `dataset.gameKey`) to update cards in-place without unmounting active focused nodes. If an active card or control is removed, the controller transfers focus to an adjacent sibling item before detachment. If an entire section or container being unmounted contains `document.activeElement` and zero surviving focusable sibling elements exist within that container zone, programmatic focus MUST be anchored and restored to `#shelf-tab-all` (or the nearest surviving section header) prior to detachment, retaining continuous keyboard focus without evicting focus to `document.body`, displacing focus to the top-level favorites grid, or causing viewport scroll jumps.
3. Folder View Rendering & Section Layout:
   - Delegate section projection to `projectLibrarySections` passing `rootPaths` and `folderAliases` queried from `options.getLibraryConfig?.()`.
    - Tab `all`: Render `.folder-accordion-toolbar` container (`role="toolbar"`, `aria-label="Folder accordion controls"`) at the top of folder sections strictly when `folderSections.length > 0` (omitted on `activeTabId === 'fav'`, individual folder tabs, and flat sort modes), containing Expand All and Collapse All buttons using `.small-btn.text-btn` (with localized text from `t('expand_all')` and `t('collapse_all')`). Render collapsible accordion sections with chevron, folder alias/title, game count. Set `sectionElement.dataset.folderPath = section.path` and attach `gridElement.dataset.rootFolderPath = section.path` via native DOM dataset properties or context-aware attribute escaping (`escapeHtml(section.path)`), prohibiting raw string interpolation of unescaped paths into HTML template literals (`data-folder-path="${section.path}"`) to eliminate DOM attribute breakout (XSS) vectors when folder paths contain quote characters on POSIX systems. Assign a deterministic unique HTML `id` attribute using the shared sanitization contract (`folder-grid-<sanitized-key>` replacing non-alphanumeric characters via `/[^a-zA-Z0-9_-]/g, '_'`) to `.game-grid` matching `aria-controls` on the toggle button, with `role="region"` and `aria-labelledby` pointing to the section toggle button ID or heading element ID per WAI-ARIA APG standards. Folder title/alias (`section.displayName`) and game count badge MUST be rendered using `textContent` on dedicated DOM elements, or sanitized through `escapeHtml()` before any HTML string interpolation, preventing Stored DOM XSS from malicious folder names.
    - WAI-ARIA Accordion Semantics & Heading Decoupling: Section header containers serve as semantic heading wrappers (`role="heading"`, `aria-level="3"`), while accordion expansion triggers are isolated toggle button controls (`role="button"`, `tabindex="0"`, dynamic `aria-expanded="true"` when open and `aria-expanded="false"` when collapsed, and `aria-controls` referencing the unique section grid ID) encompassing the chevron indicator (rendered as an accessible inline vector SVG glyph with `aria-hidden="true"` matching `.icon-svg` dimensions and bound to `currentColor`, strictly banning raw Unicode characters or CSS character content), declaring a dynamic `aria-label` (e.g. `${options.i18n?.('toggle_folder') ?? 'Toggle folder'}: ${section.displayName}`) or `aria-labelledby` referencing both the toggle trigger and the sibling folder title element identifier, paired with a visual hover/focus tooltip (`data-tooltip` matching localized toggle state). The folder title and inline rename input are rendered as independent sibling elements within the heading container, strictly prohibiting interactive element nesting (`tabindex="0"` or `<input>`) inside an ancestor declaring `role="button"`. The toggle button handles `Enter` and `Space` keydown events to toggle accordion state.
    - In-Place Accordion Toggling & Reorder Guards: Individual section collapse/expand toggles and toolbar Expand All / Collapse All actions MUST check the active reordering state (`if (options.isReordering?.() || isReordering) return;`) and temporarily block interactions (`pointer-events: none` and `aria-disabled="true"`) during in-flight section reordering (FLIP glide and IPC persistence). When not reordering, toggles execute in-place DOM class toggling (adding or removing `.is-collapsed` on the `.library-section` element and updating `aria-expanded` attributes on the toggle trigger) rather than dispatching a full `renderLibraryGrid('folder')` pass. When an accordion section is collapsed (`.is-collapsed` and `aria-expanded="false"`), its collapsible grid container and child interactive elements must be completely suppressed from the keyboard focus sequence and screen reader traversal via HTML `inert` throughout the collapsed state, and SortableJS on the collapsed `.game-grid` container is disabled (via `sortable.option('disabled', true)` or excluding collapsed containers from `options.bindGrids`), preventing redundant mousemove layout recalculations and phantom drop target calculations on zero-height grids. Discrete visibility transitions are governed declaratively by CSS transitions without imperative JavaScript timers. If `document.activeElement` is contained within the collapsible container being collapsed, the controller must programmatically anchor and restore focus to the section's accordion toggle button (`.library-section-header` toggle trigger) or the toolbar "Collapse All" button before applying the `inert` attribute, preventing keyboard focus eviction to `document.body`. Expanding the section immediately clears the `inert` attribute, re-enables SortableJS (`sortable.option('disabled', false)` or re-including in `options.bindGrids`), and restores discrete visibility declaratively via CSS transitions, binding post-animation state updates to native `transitionend` events on the target grid element. This preserves existing DOM nodes and ensures the hardware-accelerated CSS Grid transition (`grid-template-rows: 0fr <-> 1fr`) animates smoothly without visual flashes, while updating `collapsedSections` in memory for future re-render passes. Releasing overflow clipping on expanded sections once transition completes (when `.is-collapsed` is absent) ensures game card context menus (`z-index: 2000`), hover lift, and tooltips are not clipped.
   - When a folder contains zero games in unfiltered state, delegate empty prompt placeholder rendering to Ticket `02.4.2.2.3`; when empty due to an active category filter, omit the empty section or render a neutral filtered message without rendering the un-scanned manual addition prompt.
   - Individual tabs: Render flat, non-collapsible views with safe `dataset.rootFolderPath` assignment.
   - Propagate `rootFolderPath: section.path` into `createLibraryItem` options. For cards rendered in `refs.favGrid`, pre-compute canonical roots once via `const canonicalRoots = subsumeLibraryPaths(rootPaths, targetPlatform)` at the top of `renderLibraryGrid` and compute `rootFolderPath` via `mapGameToRootFolder(item.primaryGame || item, canonicalRoots, targetPlatform)` to eliminate redundant $O(N \times R^2)$ subsumption calculations across card rendering loops.
    - Multi-Grid SortableJS binding & Cross-Folder Drop Isolation: Scope `options.bindGrids` strictly to active visible, non-collapsed grid containers for the current tab view: on tab `'all'`, pass `[refs.favGrid, ...visibleNonCollapsedFolderGrids]`; on tab `'fav'`, pass strictly `[refs.favGrid]`; on an individual folder tab, pass strictly `[activeFolderGrid]` (excluding `refs.favGrid` and non-projected folder grids); or invoke `options.bindGrids?.([])` when `visibleGames.length === 0` in flat sort modes or when `rootPaths.length === 0`. To enforce cross-folder drop isolation in folder sort mode, folder grid containers must reject drops from sibling folder grids (allowing drops only within the same folder container or into `refs.favGrid`). Active Drag Teardown Invariant: prior to tearing down or rebuilding Sortable instances during re-renders, the controller checks if a drag is active, cancelling in-flight drags and purging `.sortable-drag`, `.sortable-ghost`, and `.sortable-fallback` elements from `document.body` to prevent orphaned zombie DOM clones and pointer lock traps.
4. App Composition Wiring in `src/renderer/bootstrap/app-composition.ts`:
   - Pass `gameGridWrapper: refs.gameGridWrapper` under `refs` and `getLibraryConfig: () => state.getCurrentLibraryConfig()` in options to `createLibraryGridController`.

Provide comprehensive Vitest unit tests in `src/renderer/library-grid.test.ts`.

## Blocked by
- [01.3.2 — Library Grid SortableJS Card Drag Integration & Styles ($S$)](file:///d:/Projects/YumeShelf/docs/specs/07-sort-by-folder-and-sortablejs/tickets/01.3.2-library-grid-sortablejs-card-drag-integration.md)
- [02.1 — Headless Library Section Projections & Library Stacks Custom Order ($B$)](file:///d:/Projects/YumeShelf/docs/specs/07-sort-by-folder-and-sortablejs/tickets/02.1-headless-library-sections-and-stacks.md)
- [02.4.2.1 — Folder Sort Shell Scaffolding, Markup, Styles & Locales ($B$)](file:///d:/Projects/YumeShelf/docs/specs/07-sort-by-folder-and-sortablejs/tickets/02.4.2.1-folder-sort-shell-scaffolding-and-locales.md)

## Status
todo

## Target Files

### [MODIFY] [`src/renderer/library-grid.ts`](file:///d:/Projects/YumeShelf/src/renderer/library-grid.ts)
- Implement folder accordion layout:
  - In-memory `collapsedSections` Set tracking.
  - Toggling `refs.unfavGrid.style.display` (`none` in folder mode vs `''` in flat modes), managing `refs.favGrid.style.display` (`''` on `all` and `fav` tabs, `'none'` on individual folder tabs to eliminate ghost drop zones, preserving `.drop-zone` with `min-height: 40px` when favorites count is 0 on `all` tab), managing `refs.gameGridWrapper` landmark roles in folder mode (`role="tabpanel"` and `aria-labelledby` referencing active tab ID, removed in flat sorts), and managing `refs.separator.style.display` (`flex` only on `all` tab with non-empty favorites and folder sections; `none` otherwise).
  - Reconciling dynamic `.library-section` containers in-place keyed by `dataset.folderPath` when in folder mode (updating existing sections, removing deleted roots, appending new roots), restricting unconditional removal of `.library-section` elements strictly to mode switches leaving folder mode and in `destroy()`.
  - Section projection via `projectLibrarySections`.
  - Accordion header markup with chevrons (`aria-label` or `aria-labelledby` and visual tooltip), deterministic unique `id`, `role="region"`, and `aria-labelledby` on collapsible `.game-grid`, releasing overflow clipping on expanded sections upon transition completion, and toolbar.
  - Attaching `dataset.rootFolderPath` to `.game-grid` elements and `dataset.folderPath` to `.library-section` elements via native property assignment or `escapeHtml`, rendering `section.displayName` via `textContent` or `escapeHtml`, and propagating `rootFolderPath` into `createLibraryItem` (computing `canonicalRoots = subsumeLibraryPaths(rootPaths, targetPlatform)` once before rendering cards in `favGrid` and passing to `mapGameToRootFolder`).
  - Multi-grid SortableJS binding via `options.bindGrids([refs.favGrid, ...folderGrids])` (and `options.bindGrids([])` on empty library state in flat modes or when `rootPaths.length === 0`, proceeding to section projection when `rootPaths.length > 0` in folder mode).
  - Scope empty state action buttons in `renderLibraryEmptyState` and `renderFilteredEmptyState` to `refs.emptyContainer.querySelector` rather than global `document.getElementById` for reliable headless mock DOM testability.
  - Update `queryRenderedCards()` to query `.game-card[data-game-key]` within `refs.gameGridWrapper`, and enforce container-scoped composite keying in `captureCardRects()` and `animateReorderedCards()` (combining container zone identity with `gameKey`) so that dual-placed cards in pinned favorites and folder sections maintain isolated spatial coordinates without overwrite collisions or erroneous cross-screen jump transitions.
  - Accept `getLibraryConfig?: () => any` to query `rootPaths` and `folderAliases` for section projection.

### [MODIFY] [`src/renderer/bootstrap/app-composition.ts`](file:///d:/Projects/YumeShelf/src/renderer/bootstrap/app-composition.ts)
- Update `createLibraryGridController` instantiation:
  - Pass `gameGridWrapper: refs.gameGridWrapper` under `refs`.
  - Pass `getLibraryConfig: () => state.getCurrentLibraryConfig()` in options.

### [MODIFY] [`src/renderer/library-grid.test.ts`](file:///d:/Projects/YumeShelf/src/renderer/library-grid.test.ts)
- Vitest unit tests verifying:
  1. Accordion state tracking across render passes (`collapsedSections`).
  2. Grid visibility toggling (`unfavGrid` hidden in folder sort, dynamic sections cleared on switch).
  3. Safe dataset assignment of `data-folder-path` and `data-root-folder-path` without unescaped template literal breakout.
  4. Stored DOM XSS prevention: given a folder with payload characters (`<img src=x onerror="alert(1)">`), the header renders literal text without evaluating script.
  5. Multi-grid SortableJS binding invoked with `[refs.favGrid, ...folderGridElements]` in folder sort, and with `[]` on empty library state.

## Acceptance Criteria
- [ ] Accept `refs.gameGridWrapper`, `getLibraryConfig?: () => any`, and `bindGrids` on `createLibraryGridController` options.
- [ ] Wire `gameGridWrapper: refs.gameGridWrapper` and `getLibraryConfig: () => state.getCurrentLibraryConfig()` into `createLibraryGridController` in `src/renderer/bootstrap/app-composition.ts`.
- [ ] Maintain `collapsedSections` Set tracking collapsed folder paths across render passes.
- [ ] Toggle refs.unfavGrid.style.display to 'none' in folder mode, restoring '' in flat sort modes.
- [ ] Configure refs.favGrid display: '' on tabs 'all' and 'fav', 'none' on single folder tabs, retaining .drop-zone with 40px min-height when empty on 'all'.
- [ ] Manage refs.gameGridWrapper ARIA roles: assign role="tabpanel" and aria-labelledby referencing active tab in folder mode, removing them in flat modes.
- [ ] Display #favorites-separator as 'flex' only on tab 'all' when favorites and folder sections both contain games, otherwise set to 'none'.
- [ ] Reconcile `.library-section` containers in-place keyed by `dataset.folderPath` during folder mode render passes, iterating through `canonicalRoots` in sequential order and re-appending existing containers via `appendChild` (after `.folder-accordion-toolbar`) to guarantee physical DOM element order strictly mirrors `canonicalRoots` and synchronizes on rollback (`onReorderFailed`) and external config updates (`onConfigUpdated`), applying tab-scoped visibility culling (setting `style.display = 'none'` on all folder sections on tab `'fav'`, setting `style.display = ''` on the active section and `'none'` on sibling sections on single folder tabs, and restoring `''` across all sections on tab `'all'`), restricting unconditional clearance of `.library-section` elements strictly to mode transitions leaving folder mode and in `destroy()`.
- [ ] Symmetrically reconcile child game cards within each `.library-section` (and within `refs.favGrid` for `favoritesSection.games`) to match `section.games`, pre-indexing existing card DOM elements into an $O(1)$ Map (`Map<string, HTMLElement>`) prior to iterating model games to eliminate $O(N^2)$ quadratic traversals, reusing matching card DOM nodes by re-inserting them in target sort order and instantiating new cards via `renderItem(game)` (calling `createLibraryItem(game, itemOptions)`) when no matching DOM node exists.
- [ ] Explicitly remove unreferenced, surplus, or duplicate card DOM elements (`cardEl.remove()`) within reconciled containers, ensuring no orphan DOM nodes linger across filter or sorting changes.
- [ ] Render .folder-accordion-toolbar (role="toolbar", aria-label="Folder accordion controls") at top of folder sections strictly on tab 'all' when folders exist.
- [ ] Structure section headers as semantic heading wrappers (role="heading", aria-level="3") containing isolated toggle button controls (role="button").
- [ ] Configure toggle button with aria-expanded, dynamic aria-label referencing toggle_folder, visual tooltip, and inline SVG chevron bound to currentColor.
- [ ] Assign unique HTML id (`folder-grid-<sanitized-key>`) to collapsible grid using shared sanitization contract (`/[^a-zA-Z0-9_-]/g, '_'`) matching aria-controls on toggle button, declaring role="region" and aria-labelledby pointing to header, and assert in `library-grid.test.ts` that special folder path characters generate valid DOM IDs queryable via `document.querySelector` without CSS selector errors.
- [ ] Suppress collapsed section child elements from keyboard focus and screen readers via HTML inert, restoring focus to toggle trigger if focus was inside.
- [ ] Synchronize collapsed visibility via declarative CSS transitions, releasing overflow clipping on expanded sections once transition finishes.
- [ ] Guard accordion toggles and toolbar buttons against in-flight section reordering (if isReordering return) with aria-disabled and pointer-event blocking.
- [ ] Render clean flat non-collapsible views with fixed-open sections when viewing individual folder tabs.
- [ ] Author unit test verifying elements inside collapsed accordion sections are unreachable via Tab navigation.
- [ ] Author unit test verifying child cards remain visually rendered during collapse transition and toggle buttons render inline vector SVG glyphs bound to currentColor.
- [ ] Assign `data-root-folder-path` and `data-folder-path` using native dataset property assignment or HTML-escaped attributes to eliminate XSS attribute breakout.
- [ ] Render folder title/alias (`section.displayName`) and game count badge using `textContent` or `escapeHtml` to prevent Stored DOM XSS from malicious directory names.
- [ ] Pass `rootFolderPath` to `createLibraryItem`, pre-computing `canonicalRoots = subsumeLibraryPaths(rootPaths, targetPlatform)` once to supply to `mapGameToRootFolder` when rendering favorite cards.
- [ ] Bind SortableJS strictly across active visible, non-collapsed grid containers via `options.bindGrids`: include `[refs.favGrid, ...visibleNonCollapsedFolderGrids]` on tab 'all', strictly `[refs.favGrid]` on tab 'fav', and strictly `[activeFolderGrid]` on single folder tabs (excluding `refs.favGrid` and non-projected or collapsed folder grids to eliminate phantom drop targets and mousemove recalculations), enforcing cross-folder drop isolation so cards cannot be dropped between sibling folder grids, cancelling active in-flight drags and cleaning up `.sortable-drag`, `.sortable-ghost`, `.sortable-fallback` elements from `document.body` before destroying prior instances, and invoking `bindGrids([])` on empty library in flat modes.
- [ ] When an accordion section is collapsed or expanded, dynamically update SortableJS state on its `.game-grid` container (disabling via `sortable.option('disabled', true)` or excluding from `options.bindGrids` on collapse, and re-enabling on expand) to prevent redundant bounding box layout calculations on zero-height grids.
- [ ] In `animateReorderedCards`, mandate a strict two-phase batched layout execution pattern: batch-read visible non-collapsed cards (`card.offsetParent !== null` and `.is-collapsed` absent) and compute displacement vectors in a first pass, and dispatch `card.animate(...)` in a second pass, strictly prohibiting interleaving geometry reads with animation writes.
- [ ] In folder mode with configured roots and no category filter, bypass empty state to expose empty folder sections even when visible games count is zero.
- [ ] Scope empty state action button queries within `refs.emptyContainer` using `querySelector` rather than global `document.getElementById` for reliable headless mock DOM test execution.
- [ ] Update queryRenderedCards to query cards across refs.gameGridWrapper using container-scoped composite keys combining container zone and gameKey.
- [ ] Enforce container-scoped composite keying in card rect capture and animation routines to ensure distinct coordinates for dual-placed cards, filtering out non-visible cards (`card.offsetParent === null`, collapsed `.is-collapsed` sections, or `inert` containers) in `captureCardRects()` prior to querying `card.getBoundingClientRect()`.
- [ ] Preserve focused element identity within its container zone across re-renders without evicting focus or causing viewport scroll jumps, anchoring and restoring focus to `#shelf-tab-all` (or the nearest surviving section header) prior to detachment if an unmounting section contains active focus and has zero surviving focusable siblings.
- [ ] Author unit tests in `src/renderer/library-grid.test.ts` covering accordion state, grid visibility, XSS prevention, and multi-grid binding.
- [ ] Execute automated test verification command with zero errors.

## Verification Command
```bash
npm run test:vitest -- src/renderer/library-grid.test.ts
# Expected stdout: ✓ renderer/library-grid.test.ts, exit code 0
```
