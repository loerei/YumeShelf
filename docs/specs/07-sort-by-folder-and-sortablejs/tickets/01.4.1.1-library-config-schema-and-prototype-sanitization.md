# 01.4.1.1 — Library Config Schema Extension & Prototype Sanitization ($S$)

## Epic
PRD: Sort By Folder (Variant C) & Game Card Drag-Drop Migration to SortableJS

## What to build
Extend `LibraryConfig` interface in `src/main/library-state/scanner.ts` with `folderAliases?: Record<string, string>` mapping canonical folder paths to user display names. Update `normalizeLibraryConfigShape` in `scanner.ts` to accept an explicit parameter seam `targetPlatform?: PlatformInput` (`export function normalizeLibraryConfigShape(config: any, targetPlatform?: PlatformInput): LibraryConfig`), defaulting to `resolvePlatform(targetPlatform)`, and safely sanitize and normalize `folderAliases` and `libraryPaths`: sanitize and canonically deduplicate `libraryPaths` on read/normalization using a `Set` of canonical normalized paths (`seen: Set<string>` via `normalizePathForPlatform(p, targetPlatform)`), stripping non-root trailing slashes and resolving case/separator variations so legacy configs on disk containing duplicate, trailing-slash, or case-variant paths normalize into a unique, canonical array without duplicates; guard against prototype pollution by rejecting/stripping `__proto__`, `constructor`, `prototype`, and inherited `Object` properties (using `isPlainObject(base.folderAliases)` and validating own properties), bound alias string values to a maximum of 255 characters, strip non-printable and newline control characters (`[\r\n\t\x00-\x1f]`), trim string keys and values, prune any key whose trimmed alias evaluates to empty string `""`, normalize dictionary keys via `normalizePathForPlatform(key, targetPlatform)` stripping non-root trailing slashes for canonical key indexing, and always populate `folderAliases` as a guaranteed `Record<string, string>` defaulting to `{}` when omitted or invalid.

Author comprehensive Vitest unit tests in `src/main/library-state/config.test.ts`.

## Blocked by
- [01.2 — Shared Path Subsumption Domain Logic ($S$)](file:///d:/Projects/YumeShelf/docs/specs/07-sort-by-folder-and-sortablejs/tickets/01.2-shared-path-subsumption-domain-logic.md)

## Status
todo

## Target Files

### [MODIFY] [`src/main/library-state/scanner.ts`](file:///d:/Projects/YumeShelf/src/main/library-state/scanner.ts)
- Export invariant `WRAPPER_DIRECTORY_NAMES`:
  - Add `export` to `WRAPPER_DIRECTORY_NAMES` (`export const WRAPPER_DIRECTORY_NAMES = new Set([...]);`) on line 44 so downstream modules (`actions.ts`, `migrations.ts`) can import the authoritative set of wrapper folder names without duplicate definitions or unexported symbol errors (TS2305).
- Extend `LibraryConfig` interface:
  ```typescript
  export interface LibraryConfig {
    libraryPaths: string[];
    folderAliases?: Record<string, string>;
    // ... other properties
  }
  ```
- Update `normalizeLibraryConfigShape` signature and implementation:
  - Signature: `export function normalizeLibraryConfigShape(config: any, targetPlatform?: PlatformInput): LibraryConfig`.
  - Forward `targetPlatform` into `normalizePathForPlatform(p, targetPlatform)` for `libraryPaths` and `folderAliases` key indexing, defaulting to `resolvePlatform(targetPlatform)`.
  - Sanitize and canonically deduplicate `libraryPaths` on read/normalization using a `Set` of canonical normalized paths (`seen: Set<string>` via `normalizePathForPlatform(p, targetPlatform)`), stripping non-root trailing slashes and resolving case/separator variations so legacy configs on disk containing duplicate, trailing-slash, or case-variant paths normalize into a unique, canonical array without duplicates.
  - Safely sanitize and normalize `folderAliases`: guard against prototype pollution by rejecting/stripping `__proto__`, `constructor`, `prototype`, and inherited `Object` properties (using `isPlainObject(base.folderAliases)` and validating own properties).
  - Bound alias values to a maximum of 255 characters, strip non-printable and newline control characters (`[\r\n\t\x00-\x1f]`), trim string keys and values, prune any key whose trimmed alias evaluates to empty string `""`, normalize keys with `normalizePathForPlatform(key, targetPlatform)` stripping non-root trailing slashes, and always populate `folderAliases` as a guaranteed `Record<string, string>` defaulting to `{}` when omitted or invalid.

### [NEW] [`src/main/library-state/config.test.ts`](file:///d:/Projects/YumeShelf/src/main/library-state/config.test.ts)
- Vitest unit tests covering `normalizeLibraryConfigShape` and schema sanitization:
  1. Verifies `LibraryConfig` interface declaration with `folderAliases?: Record<string, string>`.
  2. Verifies `normalizeLibraryConfigShape` guarantees `folderAliases: {}` when input config is missing `folderAliases`.
  3. Verifies `normalizeLibraryConfigShape` canonicalizes `libraryPaths` using `normalizePathForPlatform(p, targetPlatform)`, strips non-root trailing slashes, and deduplicates redundant entries.
  4. Verifies prototype pollution keys (`__proto__`, `constructor`, `prototype`) are stripped and do not pollute `Object.prototype`.
  5. Verifies alias strings exceeding 255 characters are truncated or rejected.
  6. Verifies control characters (`[\r\n\t\x00-\x1f]`) are stripped from alias strings.
  7. Verifies empty or whitespace-only aliases are pruned from `folderAliases`.
  8. Verifies dictionary keys are canonically normalized via `normalizePathForPlatform(key, targetPlatform)` with non-root trailing slashes stripped.

## Acceptance Criteria
- [ ] Export invariant `WRAPPER_DIRECTORY_NAMES` in `src/main/library-state/scanner.ts`.
- [ ] Add `folderAliases?: Record<string, string>` to `LibraryConfig` interface in `src/main/library-state/scanner.ts`.
- [ ] Update `normalizeLibraryConfigShape` signature to accept `targetPlatform?: PlatformInput`, canonically deduplicate `libraryPaths` via `normalizePathForPlatform` with non-root trailing slash stripping, strip prototype pollution keys (`__proto__`, `constructor`, `prototype`), bound alias strings to 255 characters, strip control characters (`[\r\n\t\x00-\x1f]`), prune any key whose trimmed alias evaluates to empty string `""`, normalize keys via `normalizePathForPlatform` with non-root trailing slash stripping, and guarantee a default `{}` object.
- [ ] Run automated Vitest unit tests verifying prototype pollution sanitization, trailing slash stripping, and folder alias bounding in `scanner.ts` via `npm run test:vitest -- src/main/library-state/config.test.ts -t "folderAliases"`.
- [ ] Execute automated verification command with zero errors.

## Verification Command
```bash
npm run test:vitest -- src/main/library-state/config.test.ts -t "folderAliases"
# Expected stdout: ✓ main/library-state/config.test.ts, exit code 0
```
